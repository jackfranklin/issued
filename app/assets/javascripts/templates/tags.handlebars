<div class="container">
  <div class="row">
    <div class="col-lg-6">
      <ul>
        {{#each model}}
          <li>{{#link-to "tags.show" this}}{{title}}{{/link-to}}</li>
        {{/each}}
      </ul>
    </div>
    <div class="col-lg-6">
      {{ outlet }}
    </div>
  </div>
</div>
